const DOMAIN_BASE = "brandnewconference.ru";
const DOMAIN_FULL = `https://${DOMAIN_BASE}`;
const API_URL = `https://${DOMAIN_BASE}/api`;
const FILES_URL = `https://${DOMAIN_BASE}/files`;
const AUTH_URL = `https://auth.${DOMAIN_BASE}`;
const CHAT_DOMAIN = `wss://${DOMAIN_BASE}`;
const CHAT_PORT = "4000";
const API_VERSION = 2;

export {
  DOMAIN_BASE,
  DOMAIN_FULL,
  API_URL,
  API_VERSION,
  AUTH_URL,
  FILES_URL,
  CHAT_DOMAIN,
  CHAT_PORT,
}