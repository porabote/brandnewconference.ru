import React, {Component} from "react";
import TableRow from "./table-row";
import "./table.less";

const Table = (props) => {
  return (
    <div className="grid-wrap">
      <div className="grid-hover">
        {React.Children.map(props.children, (row, index) => <TableRow {...props} {...row.props}/>)}
      </div>
    </div>
  );
}

export default Table