import React from "react";

const TableCell = (props) => {
  return (
    <span className={`grid__item ${props.className || ''}`} style={props.style || {}}>
            {props.children}
        </span>
  );
}
export default TableCell;