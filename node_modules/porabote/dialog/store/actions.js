const openDialog = (msg, approveCallback, callbackData) => {
  return {
    type: 'OPEN_DIALOG',
    payload: { msg, approveCallback, callbackData }
  }
}

const openDialog = (msg, approveCallback, callbackData) => {
  return {
    type: 'OPEN_DIALOG',
    payload: { msg, approveCallback, callbackData }
  }
}

const closeConfirm = (tabKey) => {

  return {
    type: 'CLOSE_CONFIRM',
    payload: { tabKey }
  }
}

export {
  openConfirm,
  closeConfirm,
}