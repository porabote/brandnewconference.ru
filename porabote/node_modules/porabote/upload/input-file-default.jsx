import React from "react";

const InputFileDefault = (props) => {

  return (
    <label htmlFor="upload" className="upload__label-default">
      <input
        name={`${props.name}`}
        multiple="multiple"
        className="upload__upload-input-default"
        type="file"
        id="upload"
        multiple={true}
        onChange={onSelect}
      />
      Загрузить файл
    </label>
  );
}

export default InputFileDefault;