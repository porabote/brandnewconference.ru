import React from 'react';

const FileInputCustom = (props) => {
  
  return (
    <div>
      <label htmlFor="upload" className="upload__label-custom">
        <input
          name={`${props.name}`}
          multiple="multiple"
          className="upload__upload-input-custom"
          type="file"
          id="upload"
          multiple={true}
          onChange={props.onSelect}
        />
        {props.children}
      </label>
    </div>
  );
};

export default FileInputCustom;