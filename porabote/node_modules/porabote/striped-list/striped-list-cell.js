import React from 'react'

const StripedListCell = (props) => {

    let className = props.className || 'grid_list__item';

    return(
        <span className={className} {...props}>{props.children}</span>
    )
}

export default StripedListCell